package Collection

/** collections are group of elements stored together in a single variable, similar to array
 * but they are different
 *
 * collections are List,Set,Map
 *
 * Mutable collection -> can add or modify elements after they are created
 * Immutable collection -> cannot add or modify elements after they are created,
 * its size are fixed like array
 */